let total=300,remaining=300,timer=null;function fmt(s){let m=Math.floor(s/60);let ss=s%60;return String(m).padStart(2,'0')+':'+String(ss).padStart(2,'0')}function update(){document.getElementById('time').textContent=fmt(remaining);let p=(total-remaining)/total*360;document.querySelector('.circle').style.background=`conic-gradient(red 0deg,orange 60deg,yellow 120deg,green 180deg,blue 240deg,purple ${p}deg,#ddd ${p}deg)`;if(remaining<=0){clearInterval(timer);document.getElementById('message').textContent='🎉 Great waiting! You did it!'}}function startTimer(){total=remaining=parseInt(document.getElementById('minutes').value)*60;clearInterval(timer);update();timer=setInterval(()=>{remaining--;update()},1000)}function pauseTimer(){clearInterval(timer)}function resetTimer(){clearInterval(timer);remaining=total;update()}update();